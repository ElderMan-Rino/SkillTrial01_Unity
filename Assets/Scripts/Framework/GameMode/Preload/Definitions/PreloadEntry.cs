namespace Elder.Framework.GameMode.Preload.Definitions
{
    internal readonly struct PreloadEntry
    {
        public readonly string Key;

        public PreloadEntry(string key)
        {
            Key = key;
        }
    }
}
