namespace Elder.Platform.UI.Infrastructure
{
    public class ViewModelBinder
    {

    }
}
/*
 * public class ViewModelBinder<TView, TViewModel>
    where TView : IView
    where TViewModel : IViewModel
{
    public void Bind(TView view, TViewModel viewModel)
    {
        // 바인딩 로직 구현
        // 예: 이벤트 연결, 프로퍼티 동기화 등
    }

    public void Unbind(TView view, TViewModel viewModel)
    {
        // 바인딩 해제 로직 구현
    }
}
 */
